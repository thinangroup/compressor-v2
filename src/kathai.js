import React from 'react'

function Kadhai() {
    return (
        <div className='rules-main'>
            <h3 style={{ marginBottom: '0px' }}>Why do we Compress/Optimize images?</h3>
            <p className='rules-point' style={{textIndent: '50px'}}>
                In this busy world, compression plays the major role in optimize the space and reduce the cost in cloud storage
                by reducing the size of images/photos from 200kb to 50kb/10kb. In some cases only one or two images is to be
                compressed in such case, online image compressor is used where
                the requirements are like compress to 200kb, compress to 50kb, compress to 100kb. 
                Image online optimizer are used in those cases to save time and space. In this service we compresstheimage.com
                also provide you with optimizing images, resize image(size in kb). Web image optimizer are the tools which is used
                by students, teachers for their personal and official works. Image online compress is used by some graduates
                who apply for jobs where the image is to be compressed for required size. Optimized images places major role in space
                where as different image compression techniques places a vital role in it.
            </p>
        </div>
    )

    //optimized images
    //image compression
    //optimizer Online
    //jpg image optimizer

}

export default Kadhai
